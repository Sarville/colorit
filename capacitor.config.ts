import type {CapacitorConfig} from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "ru.sarville.colorit", // must match the applicationId of the APK uploaded to RuStore Console
  appName: "Colorit",
  webDir: "out",
};

export default config;
